const initState = {
    token: '',
    isAuthenticated: false,
    error: ''
}

export default function (state = initState, action) {
    switch (action.type) {
        default:
            return state;
    }
}